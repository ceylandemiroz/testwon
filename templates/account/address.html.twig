{% extends 'base.html.twig' %}

{% block title %} Mes Addresses - Wondex{% endblock %}

{% block content %}

<a href="{{ path ('account_address_add')}}" class="btn btn-info btn-sm float-right"> Ajouter une addresse </a>

<h1> Mes Addresses </h1>
 <p> Bienvenue {{app.user.firstname}} dans votre compte. C'est dans cette espace que vous allez pouvoire  gérer toutes vos addresses. </p><br/>
 <hr>
 <a href="{{ path ('account')}}"> Retour </a>
 <hr>
 {% if app.user.addresses|length == 0 %}
   <p class="text-center">
    Vous n'avez pas encore ajouté d'addresse dans votre compte client.
    Pour en ajouter une, veuillez <a href="{{ path ('account_address_add')}}">Cliquer ici</a>.
   </p>
  {% else %} 
  
    <div class="row">
        {% for address in app.user.addresses %}
            <div class="col-md-4">
            <div class="card" style="width: 20rem;">
                <div class="card-body">
                    <h5 class="card-title">{{address.city}}</h5>
                    <p class="card-text">
                    {{address.address}}<br/>
                    {{address.country}}<br/>
                    {{address.postal}}<br/>
                    
                    </p>
                    <a href="{{ path ('account_address_edit', { 'id' : address.id })}}" class="btn btn-info">modifier</a> | 
                    <a href="{{ path ('account_address_delete', { 'id' : address.id })}}" class="btn btn-info">supprimer</a>
                </div>
            </div>
            
            </div>  
        {% endfor %}    
    </div> 
{% endif %}
 
    
 
 
{% endblock %}