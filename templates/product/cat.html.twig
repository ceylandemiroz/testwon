{% extends 'base.html.twig' %}

{% block title %}Product Controller!{% endblock %}

{% block content %}
{#<style>
ul#menu-v, #menu-v ul
{
    width:300px; /* Main Menu width */
    border:1px solid rgba(190,190,190,0.3);
    list-style:none; margin:0; padding:0;
    z-index:9;
}     

#menu-v li
{
    margin:0;padding:0;
    position:relative;    
    background-color:#DDDDDD; /*fallback color*/
	background-color:rgba(0,0,0,0.5);
    transition:background 0.5s;
}
#menu-v li:hover
{
    background-color:rgba(0,0,0,0.9);
}

#menu-v a
{
    font:normal 12px Arial;
    border-top:1px solid rgba(190,190,190,0.3);
    display:block;
    color: #fff;
    font-family: cursive;
    font-size: large;
    text-decoration: none;
    line-height: 50px;
    padding-left: 50px;
    position: relative;          
}

#menu-v li:first-child a
{
    border-top:0;
}

#menu-v a.arrow::after{
    content:'';
    position:absolute;
    display:inline;
    top:50%;
    margin-top:-4px;
    right:8px;
    border-width:4px;
    border-style:solid;
    border-color:transparent transparent transparent white;
    transition:border-color 0.5s;  
}
                
#menu-v li a.arrow:hover::after
{
    border-color:transparent transparent transparent #CCCCCC;
}
        
/*Sub level menu items
---------------------------------------*/
#menu-v li ul
{
    min-width:180px; /* Sub level menu min width */
    position:absolute;
    display:none;
    left:100%;
    top:50%; transform:translateY(-50%);
}

#menu-v li:hover > ul
{
    display:block;
}  
</style>#}
<div class="row">
  {# <div class="col-md-6">
    <h3> Liste des Cats </h3></br>
    </hr>

        <ul id="menu-v">   
                {% macro menu_categories(categories) %}
                    {% import _self as macros %}
                    {% for category in categories %}
                        <li>
                            <a href="{{ path ('catprod', { 'catid': category.id })}}" class="">{{ category.name }}</a>
                            {% if category.children is not empty %}
                                <ul class="children">
                                    {{ macros.menu_categories(category.children) }}
                                </ul>
                            {% endif %}
                        </li>
                    {% endfor %}
                {% endmacro %}

                <ul class="menu-category">
                    {{ _self.menu_categories(categories) }}
                </ul>
        </ul>

  </div>#}
    <div class="col-md-6">
    <h3> Nos Produits </h3></br>
    <div class="row product-container">
        {% for product in products %}
            <div class="col-md-6">
                <div class="product-item text-center">
                    <a href="{{ path('product', { 'slug': product.slug }) }}"><img src="/img/{{ product.illustration }}" alt="{{ product.name}}" class="img-fluid"></a>
                    <h5>{{ product.name }}</h5>
                    <span class="product-subtitle"> {{ product.subtitle}} </span>
                    <span class="product-category"> {{ product.category.name}} </span>
                </div>
            </div>
                
        {% endfor %}
    </div>

      
    
    
    </div>


</div>  

{% endblock %}